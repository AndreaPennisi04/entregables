<div>
  <h1>
    Profile User
  </h1>

  <p>{{this.name}}</p>
  <p>{{this.last_name}}</p>
  <p>{{this.email}}</p>

  <div>
    <form action="/api/session/logout" method="get">
      <br />
      <button type="submit">logout</button>
    </form>
  </div>

</div>